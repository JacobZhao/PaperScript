\documentclass{article}
\usepackage[utf8]{inputenc}
\usepackage[ruled]{algorithm2e}

\newlength\mylen
\newcommand\myinput[1]{%
  \settowidth\mylen{\KwIn{}}%
  \setlength\hangindent{\mylen}%
  \hspace*{\mylen}#1\\}


\begin{document}
\begin{algorithm}[H]
\caption{Feasibility Check}%算法名字
\LinesNumbered %要求显示行号
\SetKwInOut{Input}{Input}  
\Input{\textit{$M_{(p,i)}$}:# of modules in panel \textit{p} at current \textit{i};\\
\textit{P}: # of panels; \textit{S}: # of strings;\\
\textit{$Q_M$}: minimum # of modules per-string;}
\SetKwInOut{Output}{Output}
\Output{Feasibility Result;\\
  Conf: Configuration Result;}
\For{\textbf{\textup{each}} k \textup{(}$1 \leq k \leq S-1 $\textup{)}, \textup{Panel} p}{
Loss[\textit{p,k}] = $M_{(p,S-k)}$ -- $M_{(p,S-(k+1))}$
} \\
(${p_0}^*$,${p_1}^*$,...,${p_{P-1}}^*$) sorted by lexicographical permutation in terms of \textit{Loss}\;
% Table = lexicographical permutation ($D_p$, $m_p$)\;
% \tcp{Table have NP columns and (s+$D_p$) rows}
\For{\textbf{\textup{each}} j \textup{(}$S \geq j \geq 2$\textup{)}}{
  \For{\textbf{\textup{each}} $p^*$}{
    \eIf{$\sum$ $M_{(p^*,j)}$ \geq $Q_M$}{
      \While{$M_{({p_n}^*,j)}$ == 0, \textup{(}$0 \leq n \leq P-1 $\textup{)}}{
        release Panel ${p_n}^*$ in $p^*$}
      \eIf{$\sum$  $M_{(p^*, j)}  == $Q_M$}{
        Conf =  $M_{(p^*, j)}$}{
        \textup{\textbf{Do}} Panel Swap}
}
    {Feasibility = No}}
}
\eIf{$\sum$ $M_{(p^*,1)}$ $\geq$ $Q_M$}{
    Feasibility = Yes\;
    Conf = select + $M_{(p^*,1)}$}{
    Feasibility = No}
\end{algorithm}
% \eIf{}{}{}





\begin{algorithm}
\caption{Panel Swap}
\LinesNumbered     %% select = $M_{(p^*, j)}$\;       
\KwIn{$M_{(p^*,j)}$, $Q_M$}
\KwOut{Conf.}
Over = $\sum$  $M_{(p^*, j)}$ - $Q_M$ \;
\eIf{  $M_{({p_x}^*, j)}$ == Over, \textup{\textbf{for each}} x in $p^*$}{
  release Panel ${p_x}^*$ in $p^*$\;
  Conf = $M_{(p^*, j)}$ \;
}{
  \For{\textup{\textbf{each}} y in $p&*$}
      {\If{$\sum$ $M_{({p_y}^*, j)}$ == 3 \textbf{\textup{and each}} $M_{(y, j)}$ == 1}
        {}}}
\For{y, $(1\leq y \leq length(select))$}{
\If{$\sum$ select [y] == 3 \textbf{and} select [y]==1}{
    \For{k=j+1, k $\leq$ NP, k++}{
    \If{Table[i,k] == 3}{
    select = select + Table[i,k]}}}
\If{$\sum$ select [y] == 3  \textbf{and} select [y]==1 \textbf{and} select [y+1]=2}{
    \For{k=j+1, k $\leq$ NP, k++}{
    \If{Table[i,k] == 3}{
    select = select + Table[i,k]}}}    
\If{$\sum$ select [y] == 2 \textbf{and} select [y]==1}{
\For{k=j+1, k $\leq$ NP, k++}{
    \If{Table[i,k] == 2}{
    select = select + Table[i,k]}}}
    select$^*$ = select-select[y]\;
    }}
     Table = Table - select$^*$\;
% \If{$\sum$select $[y,y+1,...]$ == Table$[j+1]$}{
     %     release select[
     y]\;
%     select$^*$ = select - select[y]+Table$[j+1]$\;
%     Table = Table - select$^*$}
\end{algorithm}

\end{document}

